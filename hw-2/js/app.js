const userInput1 = prompt("Enter the first line:");
const userInput2 = prompt("Enter the second line:");
const userInput3 = prompt("Enter the third line:");

const concatenatedString = userInput2 + userInput3 + userInput1;


console.log("Rows are in random order:", concatenatedString);
